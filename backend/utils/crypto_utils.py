# ==========================================================
# Module: crypto_utils.py
# Purpose: Encryption and decryption helpers for sensitive data.
# Currently uses a simple stub (no real encryption).
# Later: replace with AES or Fernet.
# ==========================================================

def encrypt(text: str) -> str:
    # TODO: replace with AES/Fernet
    return text


def decrypt(text: str) -> str:
    return text
